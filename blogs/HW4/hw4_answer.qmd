---
format:
  html:
    toc: false
    page-layout: full
    theme: none
    fig-format: png
    fig-dpi: 150
    title-block-style: none
    code-fold: true
    code-summary: "Show Python code"
    include-in-header: mathjax.html
    html-math-method:
      method: mathjax
      prefer-html: false
jupyter: python3
execute:
  echo: true
  warning: false
  message: false
---

```{=html}
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body { background: #f0ece4; color: #2e2825; font-family: 'DM Sans', sans-serif; }
  #title-block-header, header#title-block-header { display: none !important; }
  a { color: inherit; text-decoration: none; }
  .nav { display: flex; justify-content: space-between; align-items: center; padding: 1.5rem 4rem; border-bottom: 1px solid #c9c2b6; background: #f0ece4; position: sticky; top: 0; z-index: 10; }
  .nav-logo { font-size: 0.85rem; letter-spacing: 0.1em; text-transform: uppercase; color: #7a3b35; }
  .nav-links { display: flex; gap: 2rem; list-style: none; }
  .nav-links a { font-size: 0.8rem; color: #8a8278; text-transform: uppercase; letter-spacing: 0.08em; padding: 0.4rem 0.75rem; transition: color 0.2s, background 0.2s, transform 0.2s; }
  .nav-links a:hover { color: #2e2825; background: #e8e3da; transform: translateY(-2px); }
  .nav-cta { background: #2e2825; color: #f0ece4; padding: 0.5rem 1.2rem; font-size: 0.8rem; letter-spacing: 0.08em; text-transform: uppercase; }
  .nav-cta:hover { background: #7a3b35; }
  .post-header { padding: 5rem 4rem 3rem; border-bottom: 1px solid #c9c2b6; max-width: 860px; }
  .post-tag { font-size: 0.7rem; color: #7a3b35; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 0.5rem; }
  .post-title { font-family: 'Playfair Display', serif; font-size: clamp(2rem, 4vw, 3.5rem); font-weight: 900; line-height: 1.1; color: #2e2825; margin-bottom: 0.5rem; }
  .post-subtitle { font-size: 1rem; color: #8a8278; margin-bottom: 1rem; font-style: italic; }
  .post-meta { font-size: 0.75rem; color: #8a8278; }
  .back-link { display: inline-block; margin: 2rem 4rem 0; font-size: 0.75rem; color: #7a3b35; border-bottom: 1px solid #c9a09b; padding-bottom: 2px; letter-spacing: 0.08em; text-transform: uppercase; }
  .back-link:hover { color: #2e2825; border-color: #2e2825; }
  .post-body { padding: 3rem 4rem 5rem; max-width: 860px; }
  .post-body h2 { font-family: 'Playfair Display', serif; font-size: 1.8rem; font-weight: 700; color: #2e2825; margin: 3rem 0 1rem; padding-bottom: 0.5rem; border-bottom: 1px solid #c9c2b6; }
  .post-body h3 { font-size: 1.1rem; font-weight: 500; color: #2e2825; margin: 2rem 0 0.75rem; }
  .post-body p { font-size: 0.95rem; color: #6b6560; line-height: 1.9; margin-bottom: 1.25rem; }
  .post-body ul, .post-body ol { padding-left: 1.5rem; margin-bottom: 1.25rem; }
  .post-body li { font-size: 0.95rem; color: #6b6560; line-height: 1.9; margin-bottom: 0.4rem; }
  .post-body strong { color: #2e2825; font-weight: 500; }
  .post-body em { color: #7a3b35; font-style: italic; }
  pre { background: #e8e3da !important; border: 1px solid #c9c2b6 !important; border-radius: 2px !important; }
  table { width: 100%; border-collapse: collapse; margin: 1.5rem 0; font-size: 0.88rem; }
  thead tr { border-bottom: 2px solid #c9c2b6; }
  th { text-align: left; padding: 0.6rem 1rem; font-weight: 500; color: #2e2825; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.06em; }
  td { padding: 0.6rem 1rem; color: #6b6560; border-bottom: 1px solid #e8e3da; }
  .footer-bar { margin: 0 4rem; padding: 2rem 0; border-top: 1px solid #c9c2b6; display: flex; justify-content: space-between; font-size: 0.7rem; color: #8a8278; }
  .post-body .cell-output { max-width: 100%; box-sizing: border-box; }
  .post-body .plotly-graph-div { max-width: 100% !important; margin: 0 auto 1.25rem; }
  @media (max-width: 800px) { .nav, .post-header, .post-body { padding-left: 1.5rem; padding-right: 1.5rem; } }
  body details:has(pre.sourceCode) { margin: 0 0 1.25rem; border: 1px solid #c9c2b6; border-radius: 2px; background: #e8e3da; }
  body details:has(pre.sourceCode) > summary { cursor: pointer; list-style: none; font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.1em; color: #f0ece4; background: #2e2825; padding: 0.55rem 1rem; }
</style>

<div class="nav">
  <span class="nav-logo">Eesha Jagdhane // Portfolio</span>
  <ul class="nav-links">
    <li><a href="../../index.html">Home</a></li>
    <li><a href="../../blogs.html">Blogs &amp; Insights</a></li>
  </ul>
  <a href="../../index.html#contact" class="nav-cta">Get in touch</a>
</div>

<div class="post-header">
  <div class="post-tag">Assignment 4 — Marketing Analytics</div>
  <div class="post-title">MaxDiff &amp; Multinomial Logit</div>
  <div class="post-subtitle">Which MSBA classes would students actually choose — if they had to?</div>
  <div class="post-meta">Eesha Jagdhane &nbsp;·&nbsp; May 25, 2026</div>
</div>

<a href="../../blogs.html" class="back-link">← Back to Blogs &amp; Insights</a>

<div class="post-body">
```

## Introduction

Ask someone to *rate* ten MBA classes on a 1–10 scale and you get polite noise. Ask them to pick the **one they want** and the **one they’d skip** from a lineup of four — now you’re measuring something real. That’s **MaxDiff** (best–worst scaling): repeated forced choices instead of pretend precision.

Our “products” are the ten core MSBA classes at Rady. Each survey screen shows four; every pick is a miniature conjoint experiment. I take the class survey data and estimate preferences three ways — from a quick scoreboard to a full choice model — and see whether the story holds up:

1. **Counts** — how often a class is picked best vs. worst when it appears on screen  
2. **Multinomial logit (MLE)** — a proper model of *how* people choose best and worst, with standard errors  
3. **Bayesian MNL** — the same model, but with a prior and MCMC so uncertainty is honest about *shares*, not just coefficients  

The methods mostly agree on who’s on top and who’s at the bottom. The fun is in the middle — and in the **interactive charts** below (hover, zoom, and toggle where it helps).

## The Data

```{python}
#| echo: true

from pathlib import Path

import numpy as np
import pandas as pd
import plotly.graph_objects as go
import plotly.io as pio
from IPython.display import HTML, Markdown, display
from scipy import optimize
from scipy.special import logsumexp
from scipy.stats import norm

DATA_DIR = Path(".")
if not (DATA_DIR / "maxdiff_design_and_choices.csv").exists():
    DATA_DIR = Path("blogs/HW4")

ACCENT = "#2e2825"
BG = "#f0ece4"
PANEL = "#e8e3da"
GRID = "#c9c2b6"
HIGHLIGHT = "#7a3b35"

SHORT_LAB = [
    "403 AI/Math", "464 SQL", "451 MFO", "452 Large Data", "453 Biz Analytics",
    "444 Consulting", "455 Customer", "454 Capstone", "495 Mkt Analytics", "457 BI Systems",
]


def markdown_table(headers: list[str], rows: list[list[str]]) -> str:
    lines = [
        "| " + " | ".join(headers) + " |",
        "| " + " | ".join(["---"] * len(headers)) + " |",
    ]
    lines.extend("| " + " | ".join(r) + " |" for r in rows)
    return "\n".join(lines)


pio.templates.default = "simple_white"
_plotly_js_done = False


def show_plotly(fig: go.Figure, height: int = 500) -> None:
    """Embed Plotly in Quarto HTML (display(fig) alone often shows nothing)."""
    global _plotly_js_done
    fig.update_layout(
        height=height,
        paper_bgcolor=BG,
        plot_bgcolor=PANEL,
        font=dict(color=ACCENT, family="DM Sans, sans-serif"),
        margin=dict(l=10, r=10, t=50, b=10),
    )
    fig.update_xaxes(gridcolor=GRID, zerolinecolor=GRID)
    fig.update_yaxes(gridcolor=GRID, zerolinecolor=GRID)
    js = "cdn" if not _plotly_js_done else False
    _plotly_js_done = True
    display(
        HTML(
            pio.to_html(
                fig,
                full_html=False,
                include_plotlyjs=js,
                config={"displayModeBar": True, "responsive": True},
            )
        )
    )


def hbar_with_ci(
    df: pd.DataFrame,
    y_col: str,
    x_col: str,
    lo_col: str,
    hi_col: str,
    title: str,
    x_label: str,
    color: str = HIGHLIGHT,
) -> None:
    d = df.sort_values(x_col)
    x = d[x_col].to_numpy()
    fig = go.Figure(
        go.Bar(
            y=d[y_col],
            x=x,
            orientation="h",
            marker_color=color,
            error_x=dict(
                type="data",
                symmetric=False,
                array=np.maximum(0, d[hi_col].to_numpy() - x),
                arrayminus=np.maximum(0, x - d[lo_col].to_numpy()),
                color=ACCENT,
                thickness=1.5,
            ),
            hovertemplate=f"<b>%{{y}}</b><br>{x_label}: %{{x:.3f}}<extra></extra>",
        )
    )
    fig.add_vline(x=0, line_dash="dash", line_color="#8a8278", line_width=1)
    fig.update_layout(title=title, xaxis_title=x_label, yaxis=dict(categoryorder="total ascending"))
    show_plotly(fig)

raw = pd.read_csv(DATA_DIR / "maxdiff_design_and_choices.csv")
raw = raw.rename(columns={"Record ID": "record_id"})

labels = pd.read_csv(DATA_DIR / "maxdiff_item_labels.csv", usecols=["Item #", "Item"])
labels = labels.rename(columns={"Item #": "item", "Item": "label"})

# Standard 4-item MaxDiff tasks (exclude placeholder item 11 on 2-item screens)
raw10 = raw.loc[raw["Item"] <= 10].copy()
task_n = raw10.groupby(["record_id", "Task"]).size().reset_index(name="n")
raw4 = raw10.merge(task_n.loc[task_n["n"] == 4], on=["record_id", "Task"])

n_resp = raw4["record_id"].nunique()
t_per = raw4.groupby("record_id")["Task"].nunique().mean()
j_per = raw4["Position"].nunique()
n_items = raw4["Item"].nunique()
```

```{python}
#| echo: false

display(
    Markdown(
        markdown_table(
            ["Quantity", "Value"],
            [
                ["Respondents ($N$)", f"{n_resp}"],
                ["MaxDiff tasks per respondent ($T$)", f"{t_per:.1f} (tasks 1–8)"],
                ["Items per task ($J$)", f"{j_per}"],
                ["Classes in the study", f"{n_items}"],
            ],
        )
    )
)
```

Think of the CSV as a stack of tiny choice menus: one row per class slot on each screen. After keeping only the **ten real MSBA classes** and **four-class screens**, the table above summarizes the study.

One wrinkle: the export lists 15 “tasks” per person, but **tasks 9–15** are odd two-item screens with a placeholder (item 11). They’re not standard MaxDiff. I use **tasks 1–8** — four classes, one best, one worst — which is what the MNL math in class assumes.

```{python}
#| echo: true

sanity = (
    raw4.groupby(["record_id", "Task"])["Response"]
    .agg(
        n_best=lambda s: (s == 1).sum(),
        n_worst=lambda s: (s == -1).sum(),
        n_zero=lambda s: (s == 0).sum(),
    )
    .reset_index()
)
n_fail = (
    (sanity["n_best"] != 1)
    | (sanity["n_worst"] != 1)
    | (sanity["n_zero"] != 2)
).sum()
print(f"Sanity-check failures: {n_fail} (expect 0)")

exposure = (
    raw4.groupby("Item")
    .size()
    .reset_index(name="times_shown")
    .merge(labels, left_on="Item", right_on="item")
)
exposure["short"] = exposure["Item"].map(lambda i: SHORT_LAB[i - 1])

display(
    Markdown(
        "**Exposure counts (balanced design)**\n\n"
        + markdown_table(
            ["Item", "Class", "Times shown"],
            [
                [str(int(r.Item)), r.short, str(int(r.times_shown))]
                for r in exposure.sort_values("Item").itertuples()
            ],
        )
    )
)
```

Exposure is beautifully balanced — each class shows up about **270** times. No class was accidentally hidden from the debate.

## Counts Analysis

Start with the scoreboard even your advisor could read in ten seconds. For each class $j$, track how often it was picked **best** and **worst** when it appeared, then subtract:

$$
\text{score}_j = \%\text{best}_j - \%\text{worst}_j.
$$

A score near **+1** means “crowd favorite whenever it’s on the menu.” Near **-1** means “please get this off my schedule.” Near **0** means fight night — the class is loved and hated in equal measure.

```{python}
#| echo: true

counts = (
    raw4.groupby("Item")
    .agg(
        shown=("Response", "size"),
        times_best=("Response", lambda s: (s == 1).sum()),
        times_worst=("Response", lambda s: (s == -1).sum()),
    )
    .reset_index()
)
counts["pct_best"] = counts["times_best"] / counts["shown"]
counts["pct_worst"] = counts["times_worst"] / counts["shown"]
counts["score"] = counts["pct_best"] - counts["pct_worst"]
counts = counts.merge(labels, left_on="Item", right_on="item")
counts["short"] = counts["Item"].map(lambda i: SHORT_LAB[i - 1])
counts = counts.sort_values("score", ascending=False)

display(
    Markdown(
        markdown_table(
            [
                "Item", "Class", "Shown", "Best", "Worst",
                "% best", "% worst", "Score",
            ],
            [
                [
                    str(int(r.Item)),
                    r.short,
                    str(int(r.shown)),
                    str(int(r.times_best)),
                    str(int(r.times_worst)),
                    f"{100 * r.pct_best:.1f}",
                    f"{100 * r.pct_worst:.1f}",
                    f"{r.score:.3f}",
                ]
                for r in counts.itertuples()
            ],
        )
    )
)
```

```{python}
#| echo: true
#| fig-cap: "Counts scores with 95% bootstrap CIs (resampling respondents, B = 1,000)."


def bootstrap_counts(
    data: pd.DataFrame, B: int = 1000, seed: int = 495
) -> tuple[np.ndarray, pd.DataFrame]:
    """Return bootstrap draw matrix (B × 10) and CI table by Item."""
    rng = np.random.default_rng(seed)
    ids = data["record_id"].unique()
    items = np.arange(1, 11)
    mat = np.full((B, len(items)), np.nan)
    for b in range(B):
        samp = rng.choice(ids, size=len(ids), replace=True)
        d = data[data["record_id"].isin(samp)]
        sc = d.groupby("Item").agg(
            shown=("Response", "size"),
            best=("Response", lambda s: (s == 1).sum()),
            worst=("Response", lambda s: (s == -1).sum()),
        )
        sc["score"] = sc["best"] / sc["shown"] - sc["worst"] / sc["shown"]
        mat[b, :] = sc.reindex(items)["score"].to_numpy()
    ci = pd.DataFrame(
        {
            "Item": items,
            "ci_lo": np.quantile(mat, 0.025, axis=0),
            "ci_hi": np.quantile(mat, 0.975, axis=0),
        }
    )
    return mat, ci


boot_mat, boot_ci = bootstrap_counts(raw4)
counts = counts.merge(boot_ci, on="Item", how="left")

hbar_with_ci(
    counts,
    y_col="short",
    x_col="score",
    lo_col="ci_lo",
    hi_col="ci_hi",
    title="Class preferences: counts analysis",
    x_label="Counts score (% best − % worst)",
)
```

Hover on any bar for the score; zoom if you want detail on the middle of the pack. The chart below zooms in on **MGTA 495**: each bar is one bootstrap resample of the counts score (1,000 redraws of the student sample). Drag to select a region or hover to see where the bulk of the distribution sits.

```{python}
#| echo: false
#| fig-cap: "Interactive bootstrap distribution for MGTA 495 counts score (B = 1,000 resamples)."

boot_495 = boot_mat[:, 8]
ci_lo_495, ci_hi_495 = np.quantile(boot_495, [0.025, 0.975])

fig_boot = go.Figure(
    go.Histogram(
        x=boot_495,
        nbinsx=24,
        marker_color=HIGHLIGHT,
        opacity=0.85,
        hovertemplate="Score: %{x:.3f}<br>Count: %{y}<extra></extra>",
    )
)
fig_boot.add_vline(x=boot_495.mean(), line_dash="dash", line_color=ACCENT, annotation_text="Mean")
fig_boot.add_vline(x=ci_lo_495, line_dash="dot", line_color="#8a8278")
fig_boot.add_vline(x=ci_hi_495, line_dash="dot", line_color="#8a8278")
fig_boot.update_layout(
    title="How stable is the #1 class? (MGTA 495 bootstrap)",
    xaxis_title="Counts score",
    yaxis_title="Bootstrap samples",
    bargap=0.05,
)
show_plotly(fig_boot, height=380)
```

**MGTA 495 Marketing Analytics** is the runaway winner — best on **54%** of screens, worst on **4%**, for a score of **0.49**. **453** and **455** trail but still feel like “first-round picks.” At the bottom, **444 Consulting** is the class people love to hate (worst **42%** of the time). **464 SQL** is the Switzerland of the portfolio: nearly zero net score, which usually means the fancier models will argue over its rank.

## From MaxDiff Data to MNL Choices

Counts are honest; they’re also wasteful. They ignore every time you *didn’t* pick a class. The multinomial logit (MNL) turns each MaxDiff screen into **two** statistical choices:

1. **Best choice** from all $J=4$ items on the screen, with utility $\beta_j$ and probability  
   $P(\text{best}=b) = \exp(\beta_b) / \sum_{j \in \text{shown}} \exp(\beta_j)$.
2. **Worst choice** from the three remaining items, with utilities **negated**:  
   $P(\text{worst}=c \mid \text{best}=b) = \exp(-\beta_c) / \sum_{j \neq b} \exp(-\beta_j)$.

One catch: utilities are only defined up to a constant. I pin **$\beta_1 = 0$** (MGTA 403 as the reference class) and estimate the other nine — standard move, same as setting the outside option to zero in class.

```{python}
#| echo: true


def build_tasks(df: pd.DataFrame) -> list[dict]:
    rows = []
    for _, g in df.groupby(["record_id", "Task"]):
        rows.append(
            {
                "items": g["Item"].astype(int).tolist(),
                "best": int(g.loc[g["Response"] == 1, "Item"].iloc[0]),
                "worst": int(g.loc[g["Response"] == -1, "Item"].iloc[0]),
            }
        )
    return rows


tasks = build_tasks(raw4)
print(f"MNL choice rows: {2 * len(tasks)} (2 per task)")


def prepare_task_arrays(task_rows: list[dict]) -> tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]:
    """Precompute 0-based indices so each likelihood call avoids Python dict work."""
    n = len(task_rows)
    items = np.zeros((n, 4), dtype=np.int32)
    best = np.zeros(n, dtype=np.int32)
    worst = np.zeros(n, dtype=np.int32)
    rem = np.zeros((n, 3), dtype=np.int32)
    for i, row in enumerate(task_rows):
        it = np.asarray(row["items"], dtype=np.int32) - 1
        jb = row["best"] - 1
        items[i] = it
        best[i] = jb
        worst[i] = row["worst"] - 1
        rem[i] = it[it != jb]
    return items, best, worst, rem


TASK_ITEMS, TASK_BEST, TASK_WORST, TASK_REM = prepare_task_arrays(tasks)
pd.DataFrame(tasks).head()
```

## MNL via Maximum Likelihood

Summing over all tasks, the log-likelihood is

$$
\ell(\boldsymbol{\beta}) = \sum_{\text{tasks}} \Big[
\beta_{j^*_b} - \log \sum_{j \in \text{shown}} e^{\beta_j}
- \beta_{j^*_w} - \log \sum_{j \in \text{shown}\setminus\{j^*_b\}} e^{-\beta_j}
\Big].
$$

I code the log-likelihood with **`logsumexp`** (so `exp` never explodes), then let `scipy.optimize.minimize` hunt for the peak. The interactive curve below is the week-5 “aha”: hold all other β’s at the MLE and slide **β₉** (495) — hover along the curve to see the log-likelihood climb to its peak.

```{python}
#| echo: true


def beta_full(b9: np.ndarray) -> np.ndarray:
    return np.concatenate([[0.0], np.asarray(b9, dtype=float)])


def log_likelihood(b9: np.ndarray) -> float:
    """Vectorized over all tasks (same math as the loop version)."""
    b = beta_full(b9)
    vb = b[TASK_ITEMS]
    best_part = b[TASK_BEST] - logsumexp(vb, axis=1)
    vw = -b[TASK_REM]
    worst_part = -b[TASK_WORST] - logsumexp(vw, axis=1)
    return (best_part + worst_part).sum()


mle_res = optimize.minimize(
    lambda x: -log_likelihood(x),
    np.zeros(9),
    method="BFGS",
)

b_hat = beta_full(mle_res.x)
se_free = np.sqrt(np.diag(mle_res.hess_inv))
se_full = np.concatenate([[0.0], se_free])
shares = np.exp(b_hat) / np.exp(b_hat).sum()

mle_tbl = pd.DataFrame({
    "Item": np.arange(1, 11),
    "beta": b_hat,
    "se": se_full,
    "share": shares,
})
mle_tbl = mle_tbl.merge(labels, left_on="Item", right_on="item")
mle_tbl["short"] = mle_tbl["Item"].map(lambda i: SHORT_LAB[i - 1])
mle_tbl = mle_tbl.sort_values("share", ascending=False)

display(
    Markdown(
        markdown_table(
            ["Item", "Class", "β̂", "SE", "Share"],
            [
                [
                    str(int(r.Item)),
                    r.short,
                    f"{r.beta:.3f}",
                    f"{r.se:.3f}",
                    f"{r.share:.3f}",
                ]
                for r in mle_tbl.itertuples()
            ],
        )
    )
)
```

```{python}
#| echo: false
#| fig-cap: "Interactive log-likelihood slice: vary β₉ (495) while other coefficients stay at the MLE."

idx_495 = 8
grid_b9 = np.linspace(mle_res.x[idx_495] - 1.2, mle_res.x[idx_495] + 0.6, 80)
grid_ll = np.array([
    log_likelihood(np.concatenate([mle_res.x[:idx_495], [g], mle_res.x[idx_495 + 1 :]]))
    for g in grid_b9
])

fig_mle = go.Figure()
fig_mle.add_trace(
    go.Scatter(
        x=grid_b9,
        y=grid_ll,
        mode="lines+markers",
        marker=dict(size=6, color=HIGHLIGHT),
        line=dict(color="#8a8278", width=2),
        name="ℓ(β)",
        hovertemplate="β₉ = %{x:.3f}<br>ℓ = %{y:.1f}<extra></extra>",
    )
)
fig_mle.add_vline(
    x=mle_res.x[idx_495],
    line_dash="dash",
    line_color=ACCENT,
    annotation_text=f"MLE β₉ = {mle_res.x[idx_495]:.2f}",
)
fig_mle.update_layout(
    title="Climbing the likelihood (other β’s fixed at MLE)",
    xaxis_title="Candidate β₉ — MGTA 495",
    yaxis_title="Log-likelihood ℓ(β)",
)
show_plotly(fig_mle, height=400)
```

```{python}
#| echo: false
#| fig-cap: "MLE shares of preference — hover for share on a hypothetical all-class screen."

fig_share = go.Figure(
    go.Bar(
        y=mle_tbl.sort_values("share")["short"],
        x=mle_tbl.sort_values("share")["share"],
        orientation="h",
        marker_color=HIGHLIGHT,
        hovertemplate="<b>%{y}</b><br>Share: %{x:.1%}<extra></extra>",
    )
)
fig_share.update_layout(
    title="MNL maximum likelihood — shares of preference",
    xaxis_title="Share of preference",
    xaxis_tickformat=".0%",
    yaxis=dict(categoryorder="total ascending"),
)
show_plotly(fig_share)
```

Every class keeps the **same rank** as the counts table — reassuring. The MNL just translates utilities into something marketers understand: **share of preference** if all ten classes appeared on one mega-screen. **495** grabs about **28%** of that imaginary demand; **444 Consulting** about **4%**.

## MNL via Bayesian Estimation

MLE gives a point. Bayes gives a *cloud*. The posterior is $\pi(\boldsymbol{\beta} \mid \mathbf{y}) \propto L(\boldsymbol{\beta})\,\pi(\boldsymbol{\beta})$ with a gentle prior $\boldsymbol{\beta} \sim N(\mathbf{0}, 10\mathbf{I})$ — wide enough to stay out of the way, tight enough to keep the sampler sane.

```{python}
#| echo: true


def log_posterior(b9: np.ndarray) -> float:
    return log_likelihood(b9) + norm.logpdf(b9, loc=0, scale=np.sqrt(10)).sum()


def metropolis_hastings(
    beta0: np.ndarray,
    n_iter: int,
    step: float,
    seed: int = 495,
    label: str = "MCMC",
    progress_every: int = 2_000,
) -> tuple[np.ndarray, float]:
    rng = np.random.default_rng(seed)
    k = len(beta0)
    draws = np.empty((n_iter, k))
    beta = beta0.copy()
    lp = log_posterior(beta)
    accept = 0
    print(f"{label}: starting {n_iter:,} iterations...", flush=True)
    for t in range(n_iter):
        beta_star = beta + rng.normal(0, step, size=k)
        lp_star = log_posterior(beta_star)
        if np.log(rng.random()) < lp_star - lp:
            beta = beta_star
            lp = lp_star
            accept += 1
        draws[t] = beta
        if progress_every and (t + 1) % progress_every == 0:
            print(
                f"{label}: {t + 1:,} / {n_iter:,} — acceptance so far {accept / (t + 1):.3f}",
                flush=True,
            )
    return draws, accept / n_iter


# Tune step size toward 25–40% acceptance
step = 0.12
for trial_i in range(6):
    trial_draws, trial_acc = metropolis_hastings(
        mle_res.x, 3000, step, seed=495 + trial_i, label=f"Tuning trial {trial_i + 1}"
    )
    print(f"step = {step:.3f}, acceptance = {trial_acc:.3f}", flush=True)
    if 0.25 <= trial_acc <= 0.40:
        break
    step = step / 2 if trial_acc < 0.25 else step * 1.15

mh_draws, accept_rate = metropolis_hastings(
    mle_res.x, n_iter=20_000, step=step, seed=495, label="Main chain"
)
print(f"Final acceptance rate: {accept_rate:.3f}", flush=True)

burn = mh_draws.shape[0] // 4
post_draws = mh_draws[burn:]

beta_mean = beta_full(post_draws.mean(axis=0))
beta_ci = np.quantile(post_draws, [0.025, 0.975], axis=0)

b_post = np.column_stack([np.zeros(post_draws.shape[0]), post_draws])
b_post -= b_post.max(axis=1, keepdims=True)
exp_b = np.exp(b_post)
share_draws = exp_b / exp_b.sum(axis=1, keepdims=True)
share_mean = share_draws.mean(axis=0)
share_ci = np.quantile(share_draws, [0.025, 0.975], axis=0)

bayes_tbl = pd.DataFrame({
    "Item": np.arange(1, 11),
    "beta_mean": beta_mean,
    "beta_lo": np.concatenate([[0.0], beta_ci[0]]),
    "beta_hi": np.concatenate([[0.0], beta_ci[1]]),
    "share_mean": share_mean,
    "share_lo": share_ci[0],
    "share_hi": share_ci[1],
})
bayes_tbl = bayes_tbl.merge(labels, left_on="Item", right_on="item")
bayes_tbl["short"] = bayes_tbl["Item"].map(lambda i: SHORT_LAB[i - 1])
bayes_tbl = bayes_tbl.sort_values("share_mean", ascending=False)

display(
    Markdown(
        markdown_table(
            ["Item", "Class", "β mean", "β 95% CI", "Share mean"],
            [
                [
                    str(int(r.Item)),
                    r.short,
                    f"{r.beta_mean:.3f}",
                    f"[{r.beta_lo:.2f}, {r.beta_hi:.2f}]",
                    f"{r.share_mean:.3f}",
                ]
                for r in bayes_tbl.itertuples()
            ],
        )
    )
)
```

A healthy MCMC trace looks like a **fuzzy caterpillar** — not a trend, not a stuck needle. Use the dropdown to switch traces; zoom into any panel to check mixing.

```{python}
#| echo: false
#| fig-cap: "Interactive MCMC traces (post burn-in) — pick a parameter from the dropdown."

trace_specs = [
    (8, "β₉ — 495 Mkt Analytics"),
    (4, "β₅ — 453 Biz Analytics"),
    (5, "β₆ — 444 Consulting"),
]
xs = np.arange(post_draws.shape[0])

fig_trace = go.Figure()
for i, (col, label) in enumerate(trace_specs):
    fig_trace.add_trace(
        go.Scatter(
            x=xs,
            y=post_draws[:, col],
            mode="lines",
            line=dict(width=0.6, color=[HIGHLIGHT, "#8a8278", ACCENT][i]),
            name=label,
            visible=(i == 0),
            hovertemplate="Iter %{x}<br>β = %{y:.3f}<extra></extra>",
        )
    )

buttons = []
for i, (_, label) in enumerate(trace_specs):
    vis = [j == i for j in range(len(trace_specs))]
    buttons.append(
        dict(label=label.split("—")[-1].strip(), method="update", args=[{"visible": vis}])
    )

fig_trace.update_layout(
    title=f"Metropolis–Hastings traces (acceptance ≈ {accept_rate:.0%})",
    xaxis_title="Iteration after burn-in",
    yaxis_title="β",
    updatemenus=[dict(type="dropdown", direction="down", x=0.0, y=1.12, buttons=buttons)],
    showlegend=False,
)
show_plotly(fig_trace, height=420)
```

```{python}
#| echo: false
#| fig-cap: "Bayesian posterior mean shares with 95% credible intervals (hover + zoom)."

hbar_with_ci(
    bayes_tbl,
    y_col="short",
    x_col="share_mean",
    lo_col="share_lo",
    hi_col="share_hi",
    title="Bayesian MNL — posterior shares",
    x_label="Posterior mean share",
)
```


Posterior means hug the MLE (β₉ ≈ **1.63** either way). The real win is on **shares**: every MCMC draw gets pushed through the softmax, so the error bars on the bar chart are *credible intervals*, not approximations from a formula you hope is right.

## Comparing the Three Methods

Same ten classes, three lenses. The table is the reference; the **interactive chart** lets you flip between counts, MLE shares, and Bayesian shares and hover for exact numbers — useful when two classes are separated by a hair.

```{python}
#| echo: true

cmp = counts[["Item", "short", "score"]].rename(columns={"score": "counts_score"})
cmp = cmp.merge(mle_tbl[["Item", "share"]].rename(columns={"share": "mle_share"}), on="Item")
cmp = cmp.merge(
    bayes_tbl[["Item", "share_mean"]].rename(columns={"share_mean": "bayes_share"}),
    on="Item",
)
cmp["counts_rank"] = cmp["counts_score"].rank(ascending=False, method="min").astype(int)
cmp["mle_rank"] = cmp["mle_share"].rank(ascending=False, method="min").astype(int)
cmp["bayes_rank"] = cmp["bayes_share"].rank(ascending=False, method="min").astype(int)
cmp = cmp.sort_values("mle_rank")

display(
    Markdown(
        markdown_table(
            [
                "Item", "Class", "Counts score", "Rank",
                "MLE share", "Rank", "Bayes share", "Rank",
            ],
            [
                [
                    str(int(r.Item)),
                    r.short,
                    f"{r.counts_score:.3f}",
                    str(int(r.counts_rank)),
                    f"{r.mle_share:.3f}",
                    str(int(r.mle_rank)),
                    f"{r.bayes_share:.3f}",
                    str(int(r.bayes_rank)),
                ]
                for r in cmp.itertuples()
            ],
        )
    )
)
```

```{python}
#| echo: false
#| fig-cap: "Interactive comparison — use the buttons to switch method; hover for exact values."

plot_cmp = cmp.sort_values("mle_rank").copy()
plot_cmp["counts_pct"] = plot_cmp["counts_score"] * 100
plot_cmp["mle_pct"] = plot_cmp["mle_share"] * 100
plot_cmp["bayes_pct"] = plot_cmp["bayes_share"] * 100

methods = [
    ("Counts", "counts_pct", "Counts score (pp)", "#8a8278", "%{x:.1f} pp"),
    ("MLE", "mle_pct", "MLE share (%)", "#7a3b35", "%{x:.1f}%"),
    ("Bayesian", "bayes_pct", "Bayesian share (%)", "#2e2825", "%{x:.1f}%"),
]

fig_p = go.Figure()
for i, (label, col, xlab, color, hov) in enumerate(methods):
    fig_p.add_trace(
        go.Bar(
            x=plot_cmp[col],
            y=plot_cmp["short"],
            orientation="h",
            name=label,
            marker_color=color,
            visible=(label == "MLE"),
            hovertemplate=f"<b>%{{y}}</b><br>{xlab}: {hov}<extra></extra>",
        )
    )

fig_p.update_layout(
    title="Same classes, three metrics — pick your lens",
    xaxis_title="Value (scale changes with method)",
    yaxis=dict(categoryorder="total ascending"),
    updatemenus=[
        dict(
            type="buttons",
            direction="right",
            x=0.0,
            y=1.14,
            xanchor="left",
            buttons=[
                dict(
                    label=m[0],
                    method="update",
                    args=[
                        {"visible": [j == i for j in range(len(methods))]},
                        {"xaxis.title.text": m[2]},
                    ],
                )
                for i, m in enumerate(methods)
            ],
        )
    ],
    showlegend=False,
)
show_plotly(fig_p)
```

**495 Marketing Analytics** is the story every method tells. **444 Consulting** and **451 MFO** bring up the rear. The interesting fights are in the mushy middle — **SQL**, **Capstone**, **BI Systems** — where a point or two of share shuffles the rank but not the vibe.

When would I use each tool in real life?

- **Counts** — executive snapshot, no software drama.  
- **MLE** — one number to put in a slide, with classical SEs.  
- **Bayes** — “what if we add a new elective?” or any question where uncertainty on **shares** matters more than uncertainty on β.

## Discussion

If you’re choosing classes in this cohort, the market has spoken: **take 495**, load up on **453 / 455 / 452**, and treat **444** as the option of last resort unless you love client-facing grind. The gap isn’t subtle — roughly **28%** MNL share vs. **4%** for consulting.

Statistically, this homework is really about **three ways to listen to the same choices**. Counts are the podcast summary; MLE is the transcript with footnotes; Bayes is the transcript plus a honest “we’re not 100% sure” margin on the quotes that matter (market shares).

Caveats worth saying out loud: these are **MSBA students who took a survey**, not a random draw of all business students. Who’s already suffered through 403, who’s targeting marketing vs. finance, and which classes they’ve heard horror stories about — all unobserved. Tasks 9–15 in the raw file still look like a different survey mode (item 11 placeholder), so I stuck to the clean eight-task MaxDiff core.

If I had another afternoon: **hierarchical MNL** (who loves 495 *too much*?), covariates like concentration and work experience, and a story for those two-item screens. For now, the traces look like caterpillars, the likelihood has a clear peak, and Marketing Analytics won — which, in a Marketing Analytics class, feels fitting.

```{=html}
</div>

<div class="footer-bar">
  <span>© 2026 Eesha Jagdhane</span>
  <span style="color: #7a3b35;">Built with Quarto</span>
  <span>San Diego, CA</span>
</div>
```
